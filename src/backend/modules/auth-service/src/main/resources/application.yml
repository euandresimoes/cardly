server:
  port: 8081

spring:
  application:
    name: auth-service
  profiles:
    active: prod
  jpa:
    hibernate:
      ddl-auto: none
  flyway:
    enabled: true
    locations:
      - classpath:db/migration